# Better Auth + Permix Example

Demonstrates how to use Permix with Better Auth's access-control layer. The example defines organization-style roles (owner, admin, member) with custom project and billing resources, then prints an access matrix showing each role's permissions.

## Run

```bash
pnpm install
pnpm start
```
